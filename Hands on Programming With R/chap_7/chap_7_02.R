# if (# Case 1: all the same) {
#   prize <- # look up the prize
# } else if ( # Case 2: all bars) {
#   prize <- # assign $5
# } else {
#   # count cherries
#   prize <- # calculate a prize
# }

# count diamonds
# double the prize if necessary

symbols = c('7', '7', '7')

symbols = c('B', 'BB', 'BBB')

symbols = c('C', 'DD', '0')

(length(unique(symbols)) == 1) 

bars <- all(symbols %in% symbols)
bars
